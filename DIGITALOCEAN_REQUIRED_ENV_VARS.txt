# Required Environment Variables for DigitalOcean App Platform
# Add these to your API service in the DigitalOcean dashboard

# Redis Configuration (if using DigitalOcean Managed Redis)
REDIS_URL=${redis-cache.DATABASE_URL}
REDIS_HOST=${redis-cache.HOSTNAME}
REDIS_PORT=${redis-cache.PORT}
REDIS_PASSWORD=${redis-cache.PASSWORD}
REDIS_SSL=true

# PostgreSQL Configuration (if using DigitalOcean Managed Database)
DATABASE_URL=${trading-db.DATABASE_URL}
DATABASE_HOST=${trading-db.HOSTNAME}
DATABASE_PORT=${trading-db.PORT}
DATABASE_NAME=${trading-db.DATABASE}
DATABASE_USER=${trading-db.USERNAME}
DATABASE_PASSWORD=${trading-db.PASSWORD}

# If using external Redis (e.g., Redis Cloud), use actual values:
# REDIS_HOST=redis-19158.c305.ap-south-1-1.ec2.redns.redis-cloud.com
# REDIS_PORT=19158
# REDIS_PASSWORD=your-actual-redis-password
# REDIS_SSL=true

# If using external PostgreSQL, use actual values:
# DATABASE_HOST=your-postgres-host.com
# DATABASE_PORT=5432
# DATABASE_NAME=your_database_name
# DATABASE_USER=your_database_user
# DATABASE_PASSWORD=your_database_password

# JWT Secret (IMPORTANT: Change this to a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-32-chars

# Additional Trading System Variables
AUTONOMOUS_TRADING_ENABLED=true
PAPER_TRADING_ENABLED=true
INITIAL_CAPITAL=100000
MAX_POSITIONS_PER_USER=5
POSITION_SIZE_LIMIT=20000
DAILY_LOSS_LIMIT=5000
MAX_ORDERS_PER_ACCOUNT=7

# TrueData Configuration (if you have credentials)
TRUEDATA_USERNAME=your_truedata_username
TRUEDATA_PASSWORD=your_truedata_password

# Zerodha Configuration (if you have credentials)
ZERODHA_API_KEY=your_zerodha_api_key
ZERODHA_API_SECRET=your_zerodha_api_secret 