# Local Development Environment Variables
# Save this as .env in your project root for local development

# Database Configuration (Local PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/trading_system_dev
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=trading_system_dev
DATABASE_USER=postgres
DATABASE_PASSWORD=password
DATABASE_SSL=disable

# Redis Configuration (Local Redis)
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_USERNAME=
REDIS_SSL=false

# Application Configuration - LOCAL PORT
JWT_SECRET=local_dev_jwt_secret_change_this_in_production
PORT=8000
APP_PORT=8000
NODE_ENV=development
ENVIRONMENT=development
DEBUG=true

# Python Configuration
PYTHONPATH=.
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1

# Application URLs - LOCAL URLS
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173
VITE_API_URL=http://localhost:8000

# Zerodha API Configuration (Same as production for testing)
ZERODHA_CLIENT_ID=QSW899
ZERODHA_API_KEY=sylcoq492qz6f7ej
ZERODHA_API_SECRET=jm3h4iejwnxr4ngmma2qxccpkhevo8sy
ZERODHA_ACCOUNT_NAME=Shyam anurag

# TrueData Configuration (Same as production for testing)
TRUEDATA_USERNAME=Trial106
TRUEDATA_PASSWORD=shyam106
TRUEDATA_PORT=8086
TRUEDATA_URL=push.truedata.in
TRUEDATA_SANDBOX=false

# Webhook Configuration
WEBHOOK_SECRET=local_webhook_secret_for_testing
ENCRYPTION_KEY=local_encryption_key_for_testing

# Trading Configuration
PAPER_TRADING=true
PAPER_TRADING_ENABLED=true
AUTONOMOUS_TRADING_ENABLED=false

# CORS Configuration - Allow local frontend
ENABLE_CORS=true
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8000

# Logging Configuration
LOG_LEVEL=DEBUG
LOG_FORMAT=text
ENABLE_METRICS=false
BACKUP_ENABLED=false

# Performance Configuration
MAX_CONNECTIONS=10
POOL_SIZE=5
CACHE_TTL=60

# n8n Integration - Local n8n instance
N8N_WEBHOOK_URL=http://localhost:5678/webhook/trading-signals
N8N_ENABLED=false
N8N_API_KEY=local_n8n_api_key

# Data Provider Webhook
DATA_PROVIDER_WEBHOOK_URL=http://localhost:8000/webhook/data
DATA_PROVIDER_AUTH_TOKEN=local_auth_token_for_testing
DATA_PROVIDER_ENABLED=false

# Database Connection Pool Configuration
DATABASE_POOL_SIZE=2
DATABASE_MAX_OVERFLOW=3
DATABASE_POOL_TIMEOUT=30
DATABASE_POOL_RECYCLE=3600
DATABASE_POOL_PRE_PING=true
DATABASE_ECHO=true

# Redis Connection Configuration
REDIS_SOCKET_TIMEOUT=5
REDIS_SOCKET_CONNECT_TIMEOUT=5
REDIS_SOCKET_KEEPALIVE=true
REDIS_SOCKET_KEEPALIVE_OPTIONS={}
REDIS_CONNECTION_POOL_KWARGS={"max_connections": 5, "retry_on_timeout": true} 